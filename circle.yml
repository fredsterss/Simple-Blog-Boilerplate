checkout:
  post:
    - git submodule sync
    - git submodule update --init

test:
  post:
    - bundle exec ruby -v

deployment:
  production:
    branch: master
    commands:
      - bundle exec middleman s3_sync
      - curl https://www.cloudflare.com/api_json.html -d "a=fpurge_ts" -d "tkn=fccd981eb428eb8cd79dd3e19e9a5c5711b47" -d "email=accounts@rainforestqa.com" -d "z=rainforestqa.com" -d "v=1"