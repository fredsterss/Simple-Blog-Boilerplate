- if current_article
  = wrap_layout :layout do
    .post
      %h1= current_article.title 
      .meta
        %span.date= current_article.metadata[:page]['date'].strftime('%A %d %b, %Y')
        %span.author= "by " + current_article.metadata[:page]['author'].capitalize
      = yield

      %a.apply-for-job{ href: "mailto:jobs@rainforestqa.com?subject=Jobs?" }
        Interesting? We're looking for hungry hackers who love building tools, open source and testing. Click here to get in touch.